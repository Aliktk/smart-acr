# Smart ACR — On-Premises GCP VM deployment
# Copy and edit this to add a new environment

ENV_NAME="On-Premises (GCP VM)"
ENV_TYPE="remote"                              # local | remote

# Remote connection (only used when ENV_TYPE=remote)
SSH_HOST="35.208.133.203"
SSH_USER="nawazktk99"
SSH_KEY=""                                     # empty = use default ~/.ssh/id_rsa or ssh-agent
SSH_PORT="22"

# Paths on the remote (or local) machine
APP_DIR="$HOME/smart-acr"
COMPOSE_FILE="infra/docker/docker-compose.onprem.yml"

# Container names (must match container_name in the compose file)
CONTAINERS="smart-acr-backend smart-acr-frontend smart-acr-postgres smart-acr-proxy"
POSTGRES_CONTAINER="smart-acr-postgres"
POSTGRES_USER="smartacr"

# Public-facing URL to verify the app responds
PUBLIC_URL="http://35.208.133.203"
API_HEALTH_PATH="/api/v1/health/ready"
NGINX_HEALTH_PATH="/nginx-health"
