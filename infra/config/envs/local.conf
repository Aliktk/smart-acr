# Smart ACR — Local development deployment (Docker Desktop / WSL)

ENV_NAME="Local Dev"
ENV_TYPE="local"

SSH_HOST=""
SSH_USER=""
SSH_KEY=""
SSH_PORT=""

APP_DIR="$(git -C "$(dirname "$0")" rev-parse --show-toplevel 2>/dev/null || echo "$HOME/smart-acr")"
COMPOSE_FILE="infra/docker/docker-compose.local.yml"

CONTAINERS="smart-acr-backend smart-acr-frontend smart-acr-postgres"
POSTGRES_CONTAINER="smart-acr-postgres"
POSTGRES_USER="postgres"

PUBLIC_URL="http://localhost:3000"
API_HEALTH_PATH="/api/v1/health/ready"
NGINX_HEALTH_PATH=""
