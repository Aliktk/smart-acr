# Smart ACR — Local Windows/Mac Testing Deployment
# HTTP only, port 8080, no TLS — mirrors the GCP setup but runs on your machine.
#
# Usage (from repo root):
#   docker compose -f infra/docker/docker-compose.local.yml --profile ops run --rm migrate
#   docker compose -f infra/docker/docker-compose.local.yml --profile ops run --rm seed   # first run only
#   docker compose -f infra/docker/docker-compose.local.yml up -d
#
# Or use the deploy script:
#   bash infra/scripts/deploy-local.sh
#
# Required env files (copy from examples and fill in secrets):
#   infra/docker/env/backend.local.env    (from backend.local.env.example)
#   infra/docker/env/frontend.local.env   (from frontend.local.env.example)
#
# App runs at: http://localhost:8080

name: smart-acr-local

services:
  postgres:
    image: postgres:16-alpine
    container_name: smart-acr-local-postgres
    restart: unless-stopped
    env_file:
      - ./env/backend.local.env
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-smart_acr}
      POSTGRES_USER: ${POSTGRES_USER:-smartacr}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-change-this-password}
    volumes:
      - pg_data_local:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 15s
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "3"
    networks:
      - internal

  migrate:
    image: smart-acr/backend:local
    container_name: smart-acr-local-migrate
    profiles: ["ops"]
    env_file:
      - ./env/backend.local.env
    environment:
      NODE_ENV: production
      PORT: 4000
      STORAGE_PATH: /var/lib/smart-acr/storage
    depends_on:
      postgres:
        condition: service_healthy
    command: ["pnpm", "--filter", "@smart-acr/backend", "prisma:migrate:deploy"]
    restart: "no"
    networks:
      - internal

  seed:
    image: smart-acr/backend:local
    container_name: smart-acr-local-seed
    profiles: ["ops"]
    env_file:
      - ./env/backend.local.env
    depends_on:
      postgres:
        condition: service_healthy
    command: ["pnpm", "--filter", "@smart-acr/backend", "prisma:seed"]
    restart: "no"
    networks:
      - internal

  backend:
    build:
      context: ../..
      dockerfile: backend/Dockerfile
    image: smart-acr/backend:local
    container_name: smart-acr-local-backend
    restart: unless-stopped
    env_file:
      - ./env/backend.local.env
    environment:
      NODE_ENV: production
      PORT: 4000
      STORAGE_PATH: /var/lib/smart-acr/storage
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - acr_storage_local:/var/lib/smart-acr/storage
    healthcheck:
      test: ["CMD-SHELL", "node -e \"fetch('http://127.0.0.1:4000/api/v1/health/ready').then((r)=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))\""]
      interval: 20s
      timeout: 5s
      retries: 6
      start_period: 30s
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "5"
    networks:
      - internal

  frontend:
    build:
      context: ../..
      dockerfile: frontend/Dockerfile
    image: smart-acr/frontend:local
    container_name: smart-acr-local-frontend
    restart: unless-stopped
    env_file:
      - ./env/frontend.local.env
    environment:
      NODE_ENV: production
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "node -e \"fetch('http://127.0.0.1:3000').then((r)=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))\""]
      interval: 20s
      timeout: 5s
      retries: 6
      start_period: 30s
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "5"
    networks:
      - internal

  reverse-proxy:
    image: nginx:1.27-alpine
    container_name: smart-acr-local-proxy
    restart: unless-stopped
    depends_on:
      frontend:
        condition: service_healthy
      backend:
        condition: service_healthy
    ports:
      - "8080:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d/smart-acr-gcp.conf:/etc/nginx/conf.d/smart-acr.conf:ro
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://127.0.0.1/nginx-health || exit 1"]
      interval: 20s
      timeout: 5s
      retries: 5
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "3"

volumes:
  pg_data_local:
    driver: local
  acr_storage_local:
    driver: local

networks:
  internal:
    driver: bridge
