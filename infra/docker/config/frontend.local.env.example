# Frontend environment — Local testing
# Copy to: infra/docker/env/frontend.local.env
# No secrets here — nginx proxies /api/ requests to the backend container.

NODE_ENV=production
NEXT_PUBLIC_API_URL=/api/v1
